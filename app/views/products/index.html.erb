


<p id="notice"><%= notice %></p>


<h1>Lista produktów</h1>

<table>


  <tbody>
    <% for product in @products %>
      <tr>
        <td><%= product.name %></td>
        <td><%= link_to 'Edycja', edit_product_path(product), class: "button radius" %></td>
        <td><%= link_to 'Usun', product, class: "button radius", method: :delete, data: { confirm: 'Czy jestes pewien?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nowy produkt', new_product_path, class: "button radius" %>